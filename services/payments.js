var express = require('express');
var router = express.Router();
//might not need... remove later if not needed...
var firebase = require('firebase');
var db = firebase.database();

self= module.exports={

};
